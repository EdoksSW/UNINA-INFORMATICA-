CREATE TABLE Corso
(
	id_corso INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
	nome VARCHAR(100) NOT NULL,
	descrizione TEXT,

	cf_staff VARCHAR(16) NOT NULL,

	CONSTRAINT fk_corso_staff
		FOREIGN KEY(cf_staff)
		REFERENCES Staff(codicefiscale)
		ON DELETE CASCADE
);